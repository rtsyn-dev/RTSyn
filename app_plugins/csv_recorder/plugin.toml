name = "Csv Recorder"
kind = "csv_recorder"
version = "0.1.0"
description = "Records incoming values to a CSV file with configurable columns."
supports_start_stop = true
supports_restart = false
extendable_inputs = true
auto_extend_inputs = true
connection_dependent = true
loads_started = false
